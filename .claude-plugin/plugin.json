{
  "name": "seo-butler",
  "description": "An expert SEO & GEO agency for developers, inside Claude Code. Reads your actual project, detects the stack, and does the technical SEO work an agency would — sitemap, robots.txt, titles and meta, canonicals, Open Graph, JSON-LD, alt text, Core Web Vitals — always behind a plan you approve, with a git-aware backup and rollback. GEO (Generative Engine Optimization) is first-class: getting you crawled and cited by ChatGPT, Claude, Perplexity and AI Overviews, not just ranked by Google. Five commands forming a loop: /seo audits, plans and applies; /seo-live proves it actually shipped by checking the deployed site's raw response; /seo-verify has the deployed site audited by outside sources — Google Search Console, OpenSEO and geodaddy — then closes the real gaps and re-runs them until they agree; /seo-watch is a read-only weekly watchdog that diffs live reality against a stored baseline and reports regressions; /seo-report reads Search Console a month later, compares it against the snapshot from when the work landed, and turns the difference into the next prioritized plan — correlating movements with its own changelog without ever claiming causation. Ships deterministic validators, so the score is computed rather than estimated and two runs on an unchanged site agree — and it measures performance with a real Chrome DevTools trace instead of guessing it from the code, so Core Web Vitals findings come with observed numbers and named causes. Because a system that verifies itself cannot find the check it forgot to write, external findings are treated as evidence rather than verdicts: each auditor's measured false positives are catalogued, corroboration between independent sources is surfaced, and nothing is fixed outside the normal plan-and-approve gate. And because the rules themselves belong to other companies and move without notice, every checklist item carries the address of the official document behind it — Google's specifications, web.dev's Core Web Vitals, and the crawler documentation from OpenAI, Anthropic, Perplexity and Common Crawl — which the specialists read before working rather than recalling from training data.",
  "version": "2.4.0",
  "author": {
    "name": "Mert Ağralı",
    "email": "mmertagrali@gmail.com"
  },
  "keywords": [
    "seo",
    "geo",
    "structured-data",
    "json-ld",
    "sitemap",
    "robots-txt",
    "core-web-vitals",
    "lighthouse",
    "search-console"
  ]
}
